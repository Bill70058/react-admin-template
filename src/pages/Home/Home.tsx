import React, { useEffect } from 'react'
function Home() {
  useEffect(() => {
    // console.log(JSON.parse(sessionStorage.getItem('userInfo') || ''))
  })
  return <div>Home</div>
}

export default Home
